/**
 * Product Skyline's shared `products.product_code` / `user_product_access.product_id`
 * identifier for this app. Keep in sync with the `writing-tower` entry in
 * project-skyline-admin's MANAGED_PRODUCTS allowlist.
 */
export const PRODUCT_CODE = "writing-tower"
